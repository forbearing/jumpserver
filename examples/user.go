package main

import "github.com/forbearing/jumpserver"

func Example_User(client *jumpserver.Client) {
	//nodes, err := jms.Node().List(nil)
	//handleErr(err)
	//for i := range nodes {
	//    fmt.Printf("%+v\n", nodes[i].PrettyString())
	//}

	//users, err := jms.User().List(nil)
	//handleErr(err)
	//for i := range users {
	//    //fmt.Printf("%+v\n", users[i])
	//    fmt.Println(users[i].Pretty())
	//}
}
